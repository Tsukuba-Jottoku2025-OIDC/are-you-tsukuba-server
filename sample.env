# OIDC / JWT verify
OIDC_ISSUER=https://YOUR-ISSUER-URL
OIDC_AUDIENCE=
OIDC_ALGORITHMS=RS256

# caching
OIDC_DISCOVERY_CACHE_TTL=3600
JWKS_CACHE_TTL=3600

# dev login (HS256) - デモ用にローカルトークンを発行したい時だけON
ENABLE_DEV_LOGIN=false
JWT_SECRET_KEY=dev-secret
JWT_ALGORITHM=HS256
JWT_EXPIRES_MINUTES=60
